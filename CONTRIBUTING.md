# Contributing

Open an issue before changing the member-extension list, identity, security posture, or release process. Keep this repository metadata-only and make runtime changes in the owning product repository.

Before submitting a change:

```powershell
npm ci
npm run release:check
```

Do not add customer material, credentials, private overlays, database snapshots, or confidential screenshots.

